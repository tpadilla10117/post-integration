/* This is a test function to see if practice using jest: */

    function sum(a,b) {
        return a + b;
    };

    module.exports= sum;

    //expect returns an "expectation" object
    //toBe uses Object.is to test exact equality.

    //toEqual checks the value of an object